<UserControl x:Class="Presentación.View_Create_Assets"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="550" d:DesignWidth="900"
             Background="Transparent">

    <UserControl.Resources>
        <Style x:Key="ModernInputStyle" TargetType="TextBox">
            <Setter Property="Height" Value="38"/>
            <Setter Property="Background" Value="#151538"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#3F3F6B"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="10,3"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="CaretBrush" Value="#E89A24"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border x:Name="BaseBorder" 
                                Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}" 
                                CornerRadius="6">
                            <ScrollViewer x:Name="PART_ContentHost" Focusable="false" HorizontalScrollBarVisibility="Hidden" VerticalScrollBarVisibility="Hidden"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="BaseBorder" Property="BorderBrush" Value="#E89A24"/>
                            </Trigger>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter TargetName="BaseBorder" Property="BorderBrush" Value="#E89A24"/>
                                <Setter TargetName="BaseBorder" Property="BorderThickness" Value="1.5"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="ComboBoxItem">
            <Setter Property="Padding" Value="10,8"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBoxItem">
                        <Border x:Name="ItemBorder" Background="{TemplateBinding Background}" Padding="{TemplateBinding Padding}" SnapsToDevicePixels="true">
                            <ContentPresenter HorizontalAlignment="Left" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="ItemBorder" Property="Background" Value="#E89A24"/>
                                <Setter Property="Foreground" Value="#060621"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="ItemBorder" Property="Background" Value="#3F3F6B"/>
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <ControlTemplate x:Key="ComboBoxToggleButton" TargetType="ToggleButton">
            <Border x:Name="ToggleButtonBorder" 
                    Background="#151538" 
                    BorderBrush="#3F3F6B" 
                    BorderThickness="1" 
                    CornerRadius="6">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition />
                        <ColumnDefinition Width="30" />
                    </Grid.ColumnDefinitions>
                    <Path x:Name="Arrow" 
                          Grid.Column="1" 
                          Fill="#A0A0B8" 
                          HorizontalAlignment="Center" 
                          VerticalAlignment="Center" 
                          Data="M 0 0 L 4 4 L 8 0 Z"/>
                </Grid>
            </Border>
            <ControlTemplate.Triggers>
                <Trigger Property="IsMouseOver" Value="true">
                    <Setter TargetName="ToggleButtonBorder" Property="BorderBrush" Value="#E89A24"/>
                    <Setter TargetName="Arrow" Property="Fill" Value="#E89A24"/>
                </Trigger>
                <Trigger Property="IsChecked" Value="true">
                    <Setter TargetName="ToggleButtonBorder" Property="BorderBrush" Value="#E89A24"/>
                </Trigger>
            </ControlTemplate.Triggers>
        </ControlTemplate>
        
        

        <Style x:Key="ModernComboStyle" TargetType="ComboBox">
            <Setter Property="Height" Value="38"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Background" Value="#151538"/>
            <Setter Property="BorderBrush" Value="#3F3F6B"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="10,0,0,0"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Auto"/>
            <Setter Property="ScrollViewer.VerticalScrollBarVisibility" Value="Auto"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid>
                            <ToggleButton x:Name="ToggleButton" 
                                          Template="{StaticResource ComboBoxToggleButton}" 
                                          Focusable="false"
                                          IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"/>

                            <ContentPresenter x:Name="ContentSite" 
                                              IsHitTestVisible="False" 
                                              Content="{TemplateBinding SelectionBoxItem}"
                                              ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                              ContentTemplateSelector="{TemplateBinding ItemTemplateSelector}"
                                              Margin="{TemplateBinding Padding}"
                                              VerticalAlignment="Center" 
                                              HorizontalAlignment="Left">
                            </ContentPresenter>

                            <Popup x:Name="Popup" 
                                   Placement="Bottom" 
                                   IsOpen="{TemplateBinding IsDropDownOpen}" 
                                   AllowsTransparency="True" 
                                   Focusable="False" 
                                   PopupAnimation="Slide">
                                <Grid x:Name="DropDown" 
                                      SnapsToDevicePixels="True" 
                                      MinWidth="{TemplateBinding ActualWidth}" 
                                      MaxHeight="{TemplateBinding MaxDropDownHeight}">
                                    <Border x:Name="DropDownBorder" 
                                            Background="#090924" 
                                            BorderBrush="#3F3F6B" 
                                            BorderThickness="1" 
                                            CornerRadius="6"
                                            Margin="0,3,0,0">
                                        <ScrollViewer Margin="0,2" SnapsToDevicePixels="True">
                                            <StackPanel IsItemsHost="True" KeyboardNavigation.DirectionalNavigation="Contained" />
                                        </ScrollViewer>
                                    </Border>
                                </Grid>
                            </Popup>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="HasItems" Value="false">
                                <Setter TargetName="DropDownBorder" Property="MinHeight" Value="95"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        
        
    </UserControl.Resources>
    
    

    <Grid Margin="10,5,10,5">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="60"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,0,25">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                <StackPanel x:Name="Step1Indicator" Orientation="Horizontal" VerticalAlignment="Center">
                    <Border x:Name="CircleStep1" Width="28" Height="28" Background="#E89A24" CornerRadius="14">
                        <TextBlock Text="1" Foreground="#060621" FontWeight="Bold" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                    <StackPanel Margin="10,0,0,0" VerticalAlignment="Center">
                        <TextBlock x:Name="TxtStep1Title" Text="Información Base" FontSize="13" FontWeight="Bold" Foreground="White"/>
                        <TextBlock Text="Datos generales del activo" FontSize="10" Foreground="#A0A0B8"/>
                    </StackPanel>
                </StackPanel>

                <Rectangle x:Name="LineConnector" Width="80" Height="2" Fill="#3F3F6B" Margin="20,0,20,0" VerticalAlignment="Center"/>

                <StackPanel x:Name="Step2Indicator" Orientation="Horizontal" VerticalAlignment="Center" Opacity="0.5">
                    <Border x:Name="CircleStep2" Width="28" Height="28" Background="#151538" CornerRadius="14" BorderBrush="#3F3F6B" BorderThickness="1">
                        <TextBlock x:Name="TxtNum2" Text="2" Foreground="#A0A0B8" FontWeight="Bold" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                    <StackPanel Margin="10,0,0,0" VerticalAlignment="Center">
                        <TextBlock x:Name="TxtStep2Title" Text="Detalles de Hardware" FontSize="13" FontWeight="Bold" Foreground="#A0A0B8"/>
                        <TextBlock Text="Especificaciones técnicas" FontSize="10" Foreground="#A0A0B8"/>
                    </StackPanel>
                </StackPanel>
            </StackPanel>
        </Grid>

        <TabControl x:Name="WizardTabControl" Grid.Row="1" Background="Transparent" BorderThickness="0">
            <TabControl.ItemContainerStyle>
                <Style TargetType="TabItem">
                    <Setter Property="Visibility" Value="Collapsed"/>
                </Style>
            </TabControl.ItemContainerStyle>

            <TabItem>
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <UniformGrid Columns="2" Margin="10,0,10,0" VerticalAlignment="Top">

                        <StackPanel Margin="15,8,15,8">
                            <TextBlock x:Name="L0" Text="Etiqueta/Código" Foreground="#A0A0B8" FontWeight="Medium" Margin="0,0,0,6"/>
                            <TextBox x:Name="TxtEtiqueta" Style="{StaticResource ModernInputStyle}"/>
                        </StackPanel>

                        <StackPanel Margin="15,8,15,8">
                            <TextBlock x:Name="L1" Text="Categoría" Foreground="#A0A0B8" FontWeight="Medium" Margin="0,0,0,6"/>
                            <ComboBox x:Name="CbCategoria" Style="{StaticResource ModernComboStyle}"/>
                        </StackPanel>

                        <StackPanel Margin="15,8,15,8">
                            <TextBlock x:Name="L2" Text="Estado Operativo" Foreground="#A0A0B8" FontWeight="Medium" Margin="0,0,0,6"/>
                            <ComboBox x:Name="CbEstado" Style="{StaticResource ModernComboStyle}">
                                <ComboBoxItem Content="En Bodega" IsSelected="True"/>
                                <ComboBoxItem Content="Asignado"/>
                                <ComboBoxItem Content="En Mantenimiento"/>
                            </ComboBox>
                        </StackPanel>

                        <StackPanel Margin="15,8,15,8">
                            <TextBlock x:Name="L3" Text="Ubicación" Foreground="#A0A0B8" FontWeight="Medium" Margin="0,0,0,6"/>
                            <ComboBox x:Name="CbUbicacion" Style="{StaticResource ModernComboStyle}"/>
                        </StackPanel>

                        <StackPanel Margin="15,8,15,8">
                            <TextBlock x:Name="L4" Text="Fecha de Registro" Foreground="#A0A0B8" FontWeight="Medium" Margin="0,0,0,6"/>
                            <DatePicker x:Name="DpFecha" Height="38" VerticalContentAlignment="Center"/>
                        </StackPanel>

                        <StackPanel Margin="15,8,15,8">
                            <TextBlock x:Name="L5" Text="Proveedor" Foreground="#A0A0B8" FontWeight="Medium" Margin="0,0,0,6"/>
                            <ComboBox x:Name="CbProveedor" Style="{StaticResource ModernComboStyle}"/>
                        </StackPanel>

                        <StackPanel Margin="15,8,15,8">
                            <TextBlock x:Name="L6" Text="Costo de Activo ($)" Foreground="#A0A0B8" FontWeight="Medium" Margin="0,0,0,6"/>
                            <TextBox x:Name="TxtCosto" Style="{StaticResource ModernInputStyle}"/>
                        </StackPanel>
                    </UniformGrid>
                </ScrollViewer>
            </TabItem>

            <TabItem>
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <UniformGrid Columns="2" Margin="10,0,10,0">
                        <StackPanel Margin="15,6,15,6">
                            <TextBlock x:Name="LH1" Text="Marca" Foreground="#A0A0B8" FontWeight="Medium" Margin="0,0,0,5"/>
                            <TextBox x:Name="TxtMarca" Style="{StaticResource ModernInputStyle}"/>
                        </StackPanel>
                        <StackPanel Margin="15,6,15,6">
                            <TextBlock x:Name="LH2" Text="Tarjeta Gráfica" Foreground="#A0A0B8" FontWeight="Medium" Margin="0,0,0,5"/>
                            <TextBox x:Name="TxtGrafica" Style="{StaticResource ModernInputStyle}"/>
                        </StackPanel>
                        <StackPanel Margin="15,6,15,6">
                            <TextBlock x:Name="LH3" Text="Modelo" Foreground="#A0A0B8" FontWeight="Medium" Margin="0,0,0,5"/>
                            <TextBox x:Name="TxtModelo" Style="{StaticResource ModernInputStyle}"/>
                        </StackPanel>
                        <StackPanel Margin="15,6,15,6">
                            <TextBlock x:Name="LH4" Text="Memoria RAM" Foreground="#A0A0B8" FontWeight="Medium" Margin="0,0,0,5"/>
                            <TextBox x:Name="TxtRam" Style="{StaticResource ModernInputStyle}"/>
                        </StackPanel>
                        <StackPanel Margin="15,6,15,6">
                            <TextBlock x:Name="LH5" Text="Número de Serie" Foreground="#A0A0B8" FontWeight="Medium" Margin="0,0,0,5"/>
                            <TextBox x:Name="TxtSerie" Style="{StaticResource ModernInputStyle}"/>
                        </StackPanel>
                        <StackPanel Margin="15,6,15,6">
                            <TextBlock x:Name="LH6" Text="Almacenamiento [1]" Foreground="#A0A0B8" FontWeight="Medium" Margin="0,0,0,5"/>
                            <TextBox x:Name="TxtDisco1" Style="{StaticResource ModernInputStyle}"/>
                        </StackPanel>
                        <StackPanel Margin="15,6,15,6">
                            <TextBlock x:Name="LH7" Text="Dirección MAC" Foreground="#A0A0B8" FontWeight="Medium" Margin="0,0,0,5"/>
                            <TextBox x:Name="TxtMac" Style="{StaticResource ModernInputStyle}"/>
                        </StackPanel>
                        <StackPanel Margin="15,6,15,6">
                            <TextBlock x:Name="LH8" Text="Almacenamiento [2]" Foreground="#A0A0B8" FontWeight="Medium" Margin="0,0,0,5"/>
                            <TextBox x:Name="TxtDisco2" Style="{StaticResource ModernInputStyle}"/>
                        </StackPanel>
                        <StackPanel Margin="15,6,15,6">
                            <TextBlock x:Name="LH9" Text="Dirección IP" Foreground="#A0A0B8" FontWeight="Medium" Margin="0,0,0,5"/>
                            <TextBox x:Name="TxtIp" Style="{StaticResource ModernInputStyle}"/>
                        </StackPanel>
                        <StackPanel Margin="15,6,15,6">
                            <TextBlock x:Name="LH10" Text="Sistema Operativo" Foreground="#A0A0B8" FontWeight="Medium" Margin="0,0,0,5"/>
                            <TextBox x:Name="TxtSo" Style="{StaticResource ModernInputStyle}"/>
                        </StackPanel>
                        <StackPanel Margin="15,6,15,6">
                            <TextBlock x:Name="LH11" Text="Procesador" Foreground="#A0A0B8" FontWeight="Medium" Margin="0,0,0,5"/>
                            <TextBox x:Name="TxtProcesador" Style="{StaticResource ModernInputStyle}"/>
                        </StackPanel>
                        <StackPanel Margin="15,6,15,6">
                            <TextBlock x:Name="LH12" Text="Resolución de Pantalla" Foreground="#A0A0B8" FontWeight="Medium" Margin="0,0,0,5"/>
                            <TextBox x:Name="TxtResolucion" Style="{StaticResource ModernInputStyle}"/>
                        </StackPanel>
                    </UniformGrid>
                </ScrollViewer>
            </TabItem>
        </TabControl>

        <Grid Grid.Row="2" Margin="15,10,15,0">
            <Button x:Name="BtnCancelar" Content="Cancelar Registro" HorizontalAlignment="Left" Width="140" Height="38" Background="Transparent" Foreground="#E53E3E" BorderBrush="#E53E3E" BorderThickness="1" Cursor="Hand" Click="BtnCancelar_Click">
                <Button.Template>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="6">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Button.Template>
            </Button>

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button x:Name="BtnSiguiente" Content="Siguiente" Width="120" Height="38" Background="#E89A24" Foreground="#060621" FontWeight="Bold" BorderThickness="0" Cursor="Hand" Click="BtnSiguiente_Click">
                    <Button.Template>
                        <ControlTemplate TargetType="{x:Type Button}">
                            <Border Background="{TemplateBinding Background}" CornerRadius="6">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                        </ControlTemplate>
                    </Button.Template>
                </Button>
                <Button x:Name="BtnAtras" Content="Anterior" Width="100" Height="38" Background="#151538" Foreground="White" BorderThickness="0" Margin="0,0,10,0" Cursor="Hand" Visibility="Collapsed" Click="BtnAtras_Click">
                    <Button.Template>
                        <ControlTemplate TargetType="{x:Type Button}">
                            <Border Background="{TemplateBinding Background}" CornerRadius="6">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                        </ControlTemplate>
                    </Button.Template>
                </Button>
            </StackPanel>
        </Grid>
    </Grid>
</UserControl>