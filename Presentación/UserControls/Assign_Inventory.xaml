<UserControl x:Class="Presentación.UserControls.Assign_Inventory"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="700" d:DesignWidth="1050"
             Background="Transparent">

    <UserControl.Resources>

        <!-- ══ SCROLLBAR CUSTOM ══ -->
        <Style x:Key="ScrollBarLineButton" TargetType="RepeatButton">
            <Setter Property="Visibility" Value="Collapsed"/>
        </Style>
        <Style x:Key="ScrollBarPageButton" TargetType="RepeatButton">
            <Setter Property="SnapsToDevicePixels" Value="True"/>
            <Setter Property="OverridesDefaultStyle" Value="True"/>
            <Setter Property="IsTabStop" Value="False"/>
            <Setter Property="Focusable" Value="False"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="RepeatButton">
                        <Border Background="Transparent"/>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="ScrollBarThumb" TargetType="Thumb">
            <Setter Property="SnapsToDevicePixels" Value="True"/>
            <Setter Property="OverridesDefaultStyle" Value="True"/>
            <Setter Property="IsTabStop" Value="False"/>
            <Setter Property="Focusable" Value="False"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Thumb">
                        <Border x:Name="Border" CornerRadius="4" Background="#1a4a7a" Margin="2"/>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="#E89A24"/>
                            </Trigger>
                            <Trigger Property="IsDragging" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="#C27E1B"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style TargetType="ScrollBar">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="SnapsToDevicePixels" Value="True"/>
            <Setter Property="OverridesDefaultStyle" Value="True"/>
            <Style.Triggers>
                <Trigger Property="Orientation" Value="Vertical">
                    <Setter Property="Width" Value="8"/>
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="ScrollBar">
                                <Grid Background="Transparent">
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="*"/>
                                        <RowDefinition Height="Auto"/>
                                    </Grid.RowDefinitions>
                                    <RepeatButton Grid.Row="0" Style="{StaticResource ScrollBarLineButton}" Command="ScrollBar.LineUpCommand"/>
                                    <Track x:Name="PART_Track" Grid.Row="1" IsDirectionReversed="True">
                                        <Track.DecreaseRepeatButton>
                                            <RepeatButton Style="{StaticResource ScrollBarPageButton}" Command="ScrollBar.PageUpCommand"/>
                                        </Track.DecreaseRepeatButton>
                                        <Track.Thumb>
                                            <Thumb Style="{StaticResource ScrollBarThumb}"/>
                                        </Track.Thumb>
                                        <Track.IncreaseRepeatButton>
                                            <RepeatButton Style="{StaticResource ScrollBarPageButton}" Command="ScrollBar.PageDownCommand"/>
                                        </Track.IncreaseRepeatButton>
                                    </Track>
                                    <RepeatButton Grid.Row="2" Style="{StaticResource ScrollBarLineButton}" Command="ScrollBar.LineDownCommand"/>
                                </Grid>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Trigger>
                <Trigger Property="Orientation" Value="Horizontal">
                    <Setter Property="Height" Value="8"/>
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="ScrollBar">
                                <Grid Background="Transparent">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <RepeatButton Grid.Column="0" Style="{StaticResource ScrollBarLineButton}" Command="ScrollBar.LineLeftCommand"/>
                                    <Track x:Name="PART_Track" Grid.Column="1" IsDirectionReversed="False">
                                        <Track.DecreaseRepeatButton>
                                            <RepeatButton Style="{StaticResource ScrollBarPageButton}" Command="ScrollBar.PageLeftCommand"/>
                                        </Track.DecreaseRepeatButton>
                                        <Track.Thumb>
                                            <Thumb Style="{StaticResource ScrollBarThumb}"/>
                                        </Track.Thumb>
                                        <Track.IncreaseRepeatButton>
                                            <RepeatButton Style="{StaticResource ScrollBarPageButton}" Command="ScrollBar.PageRightCommand"/>
                                        </Track.IncreaseRepeatButton>
                                    </Track>
                                    <RepeatButton Grid.Column="2" Style="{StaticResource ScrollBarLineButton}" Command="ScrollBar.LineRightCommand"/>
                                </Grid>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- ══ PAGER BUTTON ══ -->
        <Style x:Key="PagerButtonStyle" TargetType="Button">
            <Setter Property="Width" Value="32"/>
            <Setter Property="Height" Value="32"/>
            <Setter Property="Margin" Value="3,0"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#A0A0B8"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="BtnBorder" Background="{TemplateBinding Background}" CornerRadius="16">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="BtnBorder" Property="Background" Value="#0d3a5c"/>
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Foreground" Value="#1a4a7a"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- ══ DATAGRID ══ -->
        <Style x:Key="ModernDataGridStyle" TargetType="DataGrid">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="GridLinesVisibility" Value="None"/>
            <Setter Property="AutoGenerateColumns" Value="False"/>
            <Setter Property="IsReadOnly" Value="True"/>
            <Setter Property="CanUserAddRows" Value="False"/>
            <Setter Property="HeadersVisibility" Value="Column"/>
            <Setter Property="RowHeight" Value="50"/>
            <Setter Property="SelectionUnit" Value="FullRow"/>
        </Style>
        <Style TargetType="DataGridColumnHeader">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#A0C4E0"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Padding" Value="12,12"/>
            <Setter Property="BorderThickness" Value="0,0,0,1"/>
            <Setter Property="BorderBrush" Value="#0e3560"/>
            <Setter Property="HorizontalContentAlignment" Value="Center"/>
        </Style>
        <Style x:Key="CenterCellStyle" TargetType="TextBlock">
            <Setter Property="HorizontalAlignment" Value="Center"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="TextTrimming" Value="CharacterEllipsis"/>
        </Style>
        <Style TargetType="DataGridRow">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0,0,0,1"/>
            <Setter Property="BorderBrush" Value="#0d3a5c"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#0e3560"/>
                </Trigger>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="#1a4a7a"/>
                    <Setter Property="Foreground" Value="#E89A24"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- ══ INPUT MODERNO ══ -->
        <Style x:Key="ModernInputStyle" TargetType="TextBox">
            <Setter Property="Height" Value="36"/>
            <Setter Property="Background" Value="#0d3a5c"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#1a4a7a"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="10,3"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="CaretBrush" Value="#E89A24"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border x:Name="B" Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="6">
                            <ScrollViewer x:Name="PART_ContentHost" Focusable="false"
                                          HorizontalScrollBarVisibility="Hidden"
                                          VerticalScrollBarVisibility="Hidden"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="B" Property="BorderBrush" Value="#E89A24"/>
                            </Trigger>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter TargetName="B" Property="BorderBrush" Value="#E89A24"/>
                                <Setter TargetName="B" Property="BorderThickness" Value="1.5"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- ══ COMBO MODERNO ══ -->
        <Style TargetType="ComboBoxItem">
            <Setter Property="Padding" Value="10,8"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBoxItem">
                        <Border x:Name="ItemBorder" Background="{TemplateBinding Background}"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Left" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="ItemBorder" Property="Background" Value="#E89A24"/>
                                <Setter Property="Foreground" Value="#08213a"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="ItemBorder" Property="Background" Value="#1a4a7a"/>
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <ControlTemplate x:Key="ComboToggleBtn" TargetType="ToggleButton">
            <Border x:Name="Brd" Background="#0d3a5c" BorderBrush="#1a4a7a"
                    BorderThickness="1" CornerRadius="6">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition/>
                        <ColumnDefinition Width="30"/>
                    </Grid.ColumnDefinitions>
                    <Path x:Name="Arrow" Grid.Column="1" Fill="#A0A0B8"
                          HorizontalAlignment="Center" VerticalAlignment="Center"
                          Data="M 0 0 L 4 4 L 8 0 Z"/>
                </Grid>
            </Border>
            <ControlTemplate.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter TargetName="Brd" Property="BorderBrush" Value="#E89A24"/>
                    <Setter TargetName="Arrow" Property="Fill" Value="#E89A24"/>
                </Trigger>
                <Trigger Property="IsChecked" Value="True">
                    <Setter TargetName="Brd" Property="BorderBrush" Value="#E89A24"/>
                </Trigger>
            </ControlTemplate.Triggers>
        </ControlTemplate>
        <Style x:Key="ModernComboStyle" TargetType="ComboBox">
            <Setter Property="Height" Value="36"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Background" Value="#0d3a5c"/>
            <Setter Property="BorderBrush" Value="#1a4a7a"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="10,0,0,0"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid>
                            <ToggleButton x:Name="ToggleButton"
                                          Template="{StaticResource ComboToggleBtn}"
                                          Focusable="false"
                                          IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"/>
                            <ContentPresenter x:Name="ContentSite" IsHitTestVisible="False"
                                              Content="{TemplateBinding SelectionBoxItem}"
                                              Margin="{TemplateBinding Padding}"
                                              VerticalAlignment="Center" HorizontalAlignment="Left"/>
                            <Popup x:Name="Popup" Placement="Bottom"
                                   IsOpen="{TemplateBinding IsDropDownOpen}"
                                   AllowsTransparency="True" Focusable="False"
                                   PopupAnimation="Slide">
                                <Grid x:Name="DropDown" SnapsToDevicePixels="True"
                                      MinWidth="{TemplateBinding ActualWidth}"
                                      MaxHeight="200">
                                    <Border Background="#071d34" BorderBrush="#1a4a7a"
                                            BorderThickness="1" CornerRadius="6" Margin="0,3,0,0">
                                        <ScrollViewer Margin="0,2" SnapsToDevicePixels="True">
                                            <StackPanel IsItemsHost="True"
                                                        KeyboardNavigation.DirectionalNavigation="Contained"/>
                                        </ScrollViewer>
                                    </Border>
                                </Grid>
                            </Popup>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- ══ ACTION BUTTON ══ -->
        <Style x:Key="ActionButtonStyle" TargetType="Button">
            <Setter Property="Height" Value="36"/>
            <Setter Property="Padding" Value="16,0"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Bg" Background="{TemplateBinding Background}" CornerRadius="6">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bg" Property="Opacity" Value="0.82"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="Bg" Property="Opacity" Value="0.4"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

    </UserControl.Resources>

    <!-- Layout principal: 3 columnas — panel lateral | separador | área principal -->
    <Grid x:Name="RootGrid" Background="Transparent">
        <Grid Margin="15,10,15,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="230"/>
                <ColumnDefinition Width="15"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- ══════════════════════════════════════════════════
                 COLUMNA 0 — Panel lateral (detalle / formulario)
            ══════════════════════════════════════════════════ -->
            <Border x:Name="PanelLateralBorder" Grid.Column="0" Background="#09274c" CornerRadius="12"
                    BorderBrush="#0d3a5c" BorderThickness="1" Padding="15">
                <Grid>

                    <!-- VISTA DETALLE -->
                    <ScrollViewer x:Name="PanelDetalle" VerticalScrollBarVisibility="Auto">
                        <StackPanel>
                            <Border Width="60" Height="60" CornerRadius="30"
                                    Background="#071d34" HorizontalAlignment="Center"
                                    BorderBrush="#E89A24" BorderThickness="2" Margin="0,0,0,12">
                                <TextBlock Text="📋" FontSize="26"
                                           HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>

                            <TextBlock x:Name="TxtPanelTitulo"
                                       Text="Selecciona una asignación"
                                       FontSize="14" FontWeight="Bold"
                                       Foreground="White" TextAlignment="Center"
                                       TextWrapping="Wrap" HorizontalAlignment="Center"
                                       Margin="0,0,0,4"/>

                            <TextBlock x:Name="TxtPanelEstado" Text="—"
                                       FontSize="11" Foreground="#A0C4E0"
                                       TextAlignment="Center" HorizontalAlignment="Center"
                                       Margin="0,0,0,14"/>

                            <Separator Background="#0e3560" Margin="0,0,0,14"/>

                            <StackPanel Margin="0,0,0,10">
                                <TextBlock Text="Colaborador" FontSize="10" Foreground="#5a8ab0" FontWeight="SemiBold"/>
                                <TextBlock x:Name="TxtPanelColaborador" Text="—"
                                           FontSize="13" Foreground="White"
                                           TextWrapping="Wrap" Margin="0,2,0,0"/>
                            </StackPanel>
                            <StackPanel Margin="0,0,0,10">
                                <TextBlock Text="Activo / Equipo" FontSize="10" Foreground="#5a8ab0" FontWeight="SemiBold"/>
                                <TextBlock x:Name="TxtPanelActivo" Text="—"
                                           FontSize="13" Foreground="White"
                                           TextWrapping="Wrap" Margin="0,2,0,0"/>
                            </StackPanel>
                            <StackPanel Margin="0,0,0,10">
                                <TextBlock Text="Fecha de Asignación" FontSize="10" Foreground="#5a8ab0" FontWeight="SemiBold"/>
                                <TextBlock x:Name="TxtPanelFecha" Text="—"
                                           FontSize="13" Foreground="White" Margin="0,2,0,0"/>
                            </StackPanel>
                            <StackPanel Margin="0,0,0,0">
                                <TextBlock Text="Observaciones" FontSize="10" Foreground="#5a8ab0" FontWeight="SemiBold"/>
                                <TextBlock x:Name="TxtPanelObservaciones" Text="—"
                                           FontSize="12" Foreground="#A0C4E0"
                                           TextWrapping="Wrap" Margin="0,2,0,0"/>
                            </StackPanel>

                            <StackPanel Margin="0,16,0,0">
                                <Button x:Name="BtnNuevaAsignacion"
                                        Content="+ Nueva Asignación"
                                        Style="{StaticResource ActionButtonStyle}"
                                        Background="#E89A24" Foreground="#08213a"
                                        Margin="0,0,0,8"
                                        Click="BtnNuevaAsignacion_Click"/>
                                <Button x:Name="BtnEditarAsignacion"
                                        Content="Editar Asignación"
                                        Style="{StaticResource ActionButtonStyle}"
                                        Background="#0d3a5c" Foreground="#E89A24"
                                        IsEnabled="False"
                                        Click="BtnEditarAsignacion_Click"/>
                            </StackPanel>
                        </StackPanel>
                    </ScrollViewer>

                    <!-- VISTA FORMULARIO -->
                    <ScrollViewer x:Name="PanelFormulario" VerticalScrollBarVisibility="Auto"
                                  Visibility="Collapsed">
                        <StackPanel>
                            <TextBlock x:Name="TxtTituloPanel" Text="Nueva Asignación"
                                       FontSize="14" FontWeight="Bold" Foreground="White"
                                       Margin="0,0,0,14"/>

                            <TextBlock Text="Colaborador *" FontSize="11" Foreground="#A0C4E0" Margin="0,0,0,5"/>
                            <ComboBox x:Name="CmbColaborador"
                                      Style="{StaticResource ModernComboStyle}"
                                      Margin="0,0,0,12"/>

                            <TextBlock Text="Activo / Dispositivo *" FontSize="11" Foreground="#A0C4E0" Margin="0,0,0,5"/>
                            <ComboBox x:Name="CmbActivo"
                                      Style="{StaticResource ModernComboStyle}"
                                      Margin="0,0,0,12"/>

                            <TextBlock Text="Fecha de Asignación *" FontSize="11" Foreground="#A0C4E0" Margin="0,0,0,5"/>
                            <DatePicker x:Name="DpFechaAsignacion" Height="36" Margin="0,0,0,12"/>

                            <TextBlock Text="Observaciones" FontSize="11" Foreground="#A0C4E0" Margin="0,0,0,5"/>
                            <TextBox x:Name="TxtObservaciones"
                                     Style="{StaticResource ModernInputStyle}"
                                     Height="70" TextWrapping="Wrap"
                                     AcceptsReturn="True"
                                     VerticalContentAlignment="Top"
                                     Padding="10,8"
                                     Margin="0,0,0,16"/>

                            <Button x:Name="BtnGuardar"
                                    Content="Guardar Asignación"
                                    Style="{StaticResource ActionButtonStyle}"
                                    Background="#E89A24" Foreground="#08213a"
                                    Margin="0,0,0,8"
                                    Click="BtnGuardar_Click"/>
                            <Button x:Name="BtnCancelar"
                                    Content="Cancelar"
                                    Style="{StaticResource ActionButtonStyle}"
                                    Background="#0d3a5c" Foreground="#A0C4E0"
                                    Click="BtnCancelar_Click"/>
                        </StackPanel>
                    </ScrollViewer>

                </Grid>
            </Border>

            <!-- ══════════════════════════════════════════════════
                 COLUMNA 2 — Área principal (header + grid + paginador)
            ══════════════════════════════════════════════════ -->
            <Grid Grid.Column="2">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="55"/>
                </Grid.RowDefinitions>

                <!-- Header -->
                <Grid Grid.Row="0" Margin="0,0,0,15">
                    <StackPanel VerticalAlignment="Center">
                        <TextBlock x:Name="TxtTitulo"
                                   Text="Inventario Asignado"
                                   FontSize="18" FontWeight="Bold" Foreground="White"/>
                        <TextBlock x:Name="TxtSubtitulo"
                                   Text="Gestiona los activos a cargo de cada colaborador"
                                   FontSize="12" Foreground="#A0C4E0" Margin="0,2,0,0"/>
                    </StackPanel>

                    <!-- Barra de búsqueda — misma estructura que GestorContrasenas -->
                    <Border x:Name="BusquedaBorder"
                            HorizontalAlignment="Right"
                            Width="260" Height="36"
                            Background="#0d3a5c"
                            BorderBrush="#1a4a7a"
                            BorderThickness="1"
                            CornerRadius="6">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="35"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="🔍" Foreground="#A0C4E0"
                                       VerticalAlignment="Center" HorizontalAlignment="Center"
                                       FontSize="12"/>
                            <TextBox x:Name="TxtBuscar" Grid.Column="1"
                                     Background="Transparent" BorderThickness="0"
                                     Foreground="White" VerticalContentAlignment="Center"
                                     FontSize="13" CaretBrush="#E89A24"/>
                        </Grid>
                    </Border>
                </Grid>

                <!-- DataGrid -->
                <Border x:Name="GridBorder" Grid.Row="1" Background="#09274c" CornerRadius="8"
                        BorderBrush="#0d3a5c" BorderThickness="1" Padding="10">
                    <DataGrid x:Name="DgAsignaciones"
                              Style="{StaticResource ModernDataGridStyle}">
                        <DataGrid.Columns>

                            <DataGridTextColumn Header="Colaborador"
                                               Binding="{Binding NombreColaborador}" Width="1.5*">
                                <DataGridTextColumn.ElementStyle>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="Foreground" Value="#E89A24"/>
                                        <Setter Property="FontWeight" Value="SemiBold"/>
                                        <Setter Property="HorizontalAlignment" Value="Center"/>
                                        <Setter Property="VerticalAlignment" Value="Center"/>
                                        <Setter Property="TextTrimming" Value="CharacterEllipsis"/>
                                        <Setter Property="ToolTip" Value="{Binding NombreColaborador}"/>
                                    </Style>
                                </DataGridTextColumn.ElementStyle>
                            </DataGridTextColumn>

                            <DataGridTextColumn Header="Activo / Equipo"
                                               Binding="{Binding NombreActivo}" Width="1.5*">
                                <DataGridTextColumn.ElementStyle>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="Foreground" Value="White"/>
                                        <Setter Property="HorizontalAlignment" Value="Center"/>
                                        <Setter Property="VerticalAlignment" Value="Center"/>
                                        <Setter Property="TextTrimming" Value="CharacterEllipsis"/>
                                        <Setter Property="ToolTip" Value="{Binding NombreActivo}"/>
                                    </Style>
                                </DataGridTextColumn.ElementStyle>
                            </DataGridTextColumn>

                            <DataGridTextColumn Header="Fecha Asignación"
                                               Binding="{Binding FechaAsignacion, StringFormat=dd/MM/yyyy}"
                                               Width="1.2*" ElementStyle="{StaticResource CenterCellStyle}"/>

                            <DataGridTextColumn Header="Estado"
                                               Binding="{Binding Estado}" Width="1*">
                                <DataGridTextColumn.ElementStyle>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="HorizontalAlignment" Value="Center"/>
                                        <Setter Property="VerticalAlignment" Value="Center"/>
                                        <Setter Property="FontWeight" Value="Medium"/>
                                        <Setter Property="Foreground" Value="White"/>
                                        <Style.Triggers>
                                            <Trigger Property="Text" Value="Asignado">
                                                <Setter Property="Foreground" Value="#4CD964"/>
                                            </Trigger>
                                            <Trigger Property="Text" Value="Devuelto">
                                                <Setter Property="Foreground" Value="#A0C4E0"/>
                                            </Trigger>
                                        </Style.Triggers>
                                    </Style>
                                </DataGridTextColumn.ElementStyle>
                            </DataGridTextColumn>

                            <DataGridTextColumn Header="Observaciones"
                                               Binding="{Binding Observaciones}" Width="1.8*">
                                <DataGridTextColumn.ElementStyle>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="Foreground" Value="#A0C4E0"/>
                                        <Setter Property="HorizontalAlignment" Value="Center"/>
                                        <Setter Property="VerticalAlignment" Value="Center"/>
                                        <Setter Property="TextTrimming" Value="CharacterEllipsis"/>
                                        <Setter Property="ToolTip" Value="{Binding Observaciones}"/>
                                    </Style>
                                </DataGridTextColumn.ElementStyle>
                            </DataGridTextColumn>

                        </DataGrid.Columns>
                    </DataGrid>
                </Border>

                <!-- Paginador -->
                <Grid Grid.Row="2" Margin="0,10,0,0">
                    <TextBlock x:Name="TxtInfoPagina" Text="Página 1 de 1"
                               Foreground="#A0C4E0" FontSize="13"
                               HorizontalAlignment="Left" VerticalAlignment="Center"/>
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
                        <Button x:Name="BtnPaginaAnterior" Style="{StaticResource PagerButtonStyle}">
                            <TextBlock Text="‹" FontSize="18" FontWeight="Bold" Margin="0,0,0,2"/>
                        </Button>
                        <StackPanel x:Name="PnlNumerosPagina" Orientation="Horizontal"/>
                        <Button x:Name="BtnPaginaSiguiente" Style="{StaticResource PagerButtonStyle}">
                            <TextBlock Text="›" FontSize="18" FontWeight="Bold" Margin="0,0,0,2"/>
                        </Button>
                    </StackPanel>
                    <TextBlock x:Name="TxtContadorRegistros" Text="Mostrando 0 registros"
                               Foreground="#A0C4E0" FontSize="12"
                               HorizontalAlignment="Right" VerticalAlignment="Center"/>
                </Grid>

            </Grid>
        </Grid>
    </Grid>
</UserControl>
